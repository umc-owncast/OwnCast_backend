package com.umc.owncast.domain.cast.service;

public interface CastSearchService {
//    public List<CastDTO.CastHomeDTO> getHomeCast(Integer page);
    // 1. Token을 이용해서 사용자의 정보 받아오기
    // 2. main_prefer 엔티티에서 해당 사용자의 주 카테고리 가져오기
    // 3. cast_main_category에서 해당 category에 해당하는 cast 뽑아오기

//    public List<CastDTO.CastHomeDTO> getCast(String keyword);
}
